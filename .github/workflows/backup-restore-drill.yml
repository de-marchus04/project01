name: backup-restore-drill
# Этот воркфлоу временно отключён — скрипты резервного копирования (Infra/ops/)
# ещё не настроены в текущей архитектуре (Vercel + Neon).
# Включите его после подготовки инфраструктурных скриптов.

on:
  schedule:
    - cron: '0 4 1 * *'
  workflow_dispatch:

jobs:
  drill:
    if: false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "Backup drill not yet configured."
